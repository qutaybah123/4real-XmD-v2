// Define channelInfo object
const channelInfo = {
    contextInfo: {
        forwardingScore: 999, // High forwarding score
        isForwarded: true,    // Mark message as forwarded
    }
};

// Export channelInfo
module.exports = { channelInfo };
